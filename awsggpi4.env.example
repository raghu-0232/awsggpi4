# Environment configuration for awsggpi4 YOLO Object Detection
# Copy this file to /etc/default/awsggpi4 and customize as needed
# sudo cp awsggpi4.env.example /etc/default/awsggpi4
# sudo nano /etc/default/awsggpi4

# Camera source configuration
# Set USE_PICAMERA=1 to use Raspberry Pi Camera, or 0 for RTSP stream
USE_PICAMERA=1

# RTSP URL (only used if USE_PICAMERA=0)
RTSP_URL=rtsp://user:password@192.168.88.44:554/cam/realmonitor?channel=1&subtype=0

# Frame dimensions
FRAME_WIDTH=1280
FRAME_HEIGHT=720

# ROI (Region of Interest) configuration
# Format: x1,y1,x2,y2 (top-left and bottom-right corners)
ROI1=0,0,640,720
ROI2=640,0,1280,720

# YOLO Model path
YOLO_MODEL_PATH=/home/pi4/screenshots/yolov8n.pt

# Stream configuration
STREAM_PORT=9090

# Inference settings
INFER_IMG_SIZE=640
INFER_EVERY_N=1

# Detection settings
COUNT_CLASS_IDS=0,1,3
DRAW_DETECTIONS=1

# Tracking settings
TRACK_IOU_THRESHOLD=0.3
TRACK_MAX_AGE_SECONDS=5

# Hourly counting settings
COUNT_INTERVAL_MINUTES=30
TZ_OFFSET_MINUTES=330
HOURLY_CSV_PATH=/home/pi4/awsggpi4/hourly_counts.csv

# ROI config file (JSON format, optional - overrides ROI1/ROI2 if exists)
ROI_CONFIG_PATH=/home/pi4/awsggpi4/roi_config.json

# Display settings (set to 1 only if you have a display connected)
ENABLE_IMSHOW=0

# JPEG quality for stream (1-100, higher = better quality but more bandwidth)
# Not set here, defaults to 85 in code
